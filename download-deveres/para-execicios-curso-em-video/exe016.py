from math import trunc
numero = float(input('digite uma numero, para ser quebrado :'))
quebrado = trunc(numero)
print('seu numero sem ta quebrado e {}, o numero quebrado e {}'.format(numero, quebrado))
